---
import { css } from "@styled-system/css";

const className = css({
  textStyle: { base: "heading-lg", sm: "display-lg" },
  paddingBlock: "2",
});
---

<h2 class={className} {...Astro.props}>
  <slot />
</h2>
